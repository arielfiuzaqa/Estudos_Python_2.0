BANCO DE DADOS EXPLICAÇÕES INICIAIS

Diferenças entre Memória Ram e Memória HD/SSD
- Memória Ram: Memória de Curto Prazo mas com velocidade de acesso muito alta. 
    Acessos a informações muito rapidamente, alta latência.

- Memória de Longo Prazo: É uma Memória longa mas em compensação é lenta, o que dificulta
ler e acessar os dados rapidamente.
    HD: Disco girando com uma agulha, o disco tem que girar ate pegar todas as informações
    que precisa. 

    SSD: Não é uma peça girando, dependendo de eletricidade para funcionar mas ainda assim É
    Muito mais rápido que o HD mas mesmo assim ainda é mais lenta do que uma Memória Ram
    por ser uma Memória de longo prazo.



Banco de Dados

Quando um usuário utiliza e cadastrar certas infomrmações para usar depois novamente queremos
que esteja lá todos os dados e em alguns projetos fizemos isso em documentos .txt e documentos
.json onde qualquer um pode ir lá e abrir o arquivo e apagar ou simplesmente ver dados sensiveis
o que pode ter problemas com segurança e perda de dados sensíveis durante os processos de armazenamentos. 
Com a estrutura de banco de dados podemos ter uma melhor relação entre tabelas seguranças, niveis de 
acessos e muitos outros recursos que potencializam a solidez das informações. Fora isso, temos ainda que
pela robustez das linguagens mães dos bancos de dados fica mais dificil apagar ou alterar alguma informação
e também para ter alta performance com milhares de dados extremamente alta.

OBS: No curso vamos ter uma  palhinha de banco de dados SQL mas não vai ser tão aprofundada o que não quer 
que eu não deva aprender mais sobre para trabalhar melhor dentro das empresas que possuem Legados. Pois vamos
usar Django e Python comandos como ORM () vamos fazer queries no nosso banco de dados, consultas e manipulações
atraves da linguagem Python.


🎯 1. Banco de Dados Relacional (RDBMS)

✅ Conceito:

Baseados em tabelas (linhas e colunas).
Cada tabela representa uma entidade (clientes, pedidos, produtos...).
As tabelas podem se relacionar entre si por chaves primárias e chaves estrangeiras.
Usam SQL (Structured Query Language) para manipular os dados.

✅ Principais características:

Estrutura rígida (esquema fixo).
Forte consistência de dados (ACID properties: Atomicidade, Consistência, Isolamento, Durabilidade).
Ideal para transações financeiras, ERP, sistemas de controle de estoque, sistemas bancários.

✅ Exemplos de bancos de dados relacionais:

Banco de Dados	        Característica Especial
MySQL	                Popular em aplicações web.
PostgreSQL	            Focado em consistência e recursos avançados.
SQLite	                Banco de dados embutido em aplicativos locais.
Oracle Database	        Robusto para grandes corporações.
Microsoft SQL Server	Integrado com soluções Microsoft.



🎯 2. Banco de Dados Não Relacional (NoSQL)

✅ Conceito:

Baseados em estruturas flexíveis: documentos, grafos, colunas, pares chave-valor.
Sem necessidade de estrutura rígida (sem esquemas fixos).
Focados em velocidade, escala horizontal (adicionar servidores facilmente) e flexibilidade.

✅ Tipos de bancos não relacionais:

Tipo	        Estrutura	                                Exemplo
Documentos	    JSON ou BSON	                            MongoDB
Chave-Valor	    Pares simples de chave e valor	Redis,      DynamoDB
Colunar	        Armazenam dados em colunas, não em linhas	Apache Cassandra
Grafos	        Representam dados em grafos (nós e arestas)	Neo4j

✅ Principais características:
Escalabilidade horizontal fácil.
Flexível (não precisa definir esquema antes).
Ideal para aplicações web de alta performance, big data, redes sociais, sistemas de recomendação.


⚡ Resumindo a Diferença:

Aspecto         | Banco Relacional                                 | Banco Não Relacional
Estrutura       | Tabelas fixas (linhas e colunas)                 | Estruturas flexíveis (documentos, grafos, chave-valor)
Linguagem       | SQL                                              | Varia: consultas específicas (ex: MongoDB Query Language)
Rigor           | Alta consistência e integridade de dados         | Flexibilidade e velocidade
Escalabilidade  | Principalmente vertical (máquinas mais potentes) | Principalmente horizontal (mais máquinas)
Uso Ideal       | Finanças, ERP, CRM, sistemas críticos            | Big data, apps em nuvem, IoT, redes sociais


🔥 Exemplos concretos:

Situação                                        | Banco Indicado
Gerenciar sistema bancário, controle de estoque | Relacional (MySQL, PostgreSQL)
Construir rede social com milhões de usuários   | Não Relacional (MongoDB, Cassandra)
Armazenar histórico de login rápido             | Não Relacional (Redis)
Gerenciar sistema de vendas para e-commerce     | Relacional (PostgreSQL)
Analisar conexões de amigos (redes sociais)     | Não Relacional (Neo4j - Grafos)



SGBD - Sistema Gerenciador de Banco de Dados
Temos varios comandos já salvos de criação de bancos e outros, como não conseguimos instalar o banco de dados
nesse computador da empresa vamos simplesmente fazer a base para usar no pc particular.
Aprendendo a montar tudo direitinho mas sem precisar de uma conexão em si.


ORM'S e ao SQLAlchemy
1 - Introdução a ORM'S e ao SQLAlchemy
Criando Tabela atraves de uma classe da linguagem Python, pois se chama Objeto com Banco de Dados Relacional
Já vem de integração orientada a Objetos, assim podemos entender uma classe de Python como uma tabela do
Banco de Dados. 
Para declaração de constantes que não devem ser mudadas com LETRAS MAIUSCULAS TODAS.
Por motivos de acesso não estou conseguindo fazer diretamente por um banco de Dado como MySQL então estou usando o SQLite.
Mas isso não impede nada de critico nesse problema

2 - Teoria sobre Sessions e inserções em tabela
Estamos criando um ORM2.py para trabalhar algumas inerções na tabela que criamos no ORM.py
pois podemos criar ORMs diferente para objetivos diferentes, no caso o primeiro deixamos para
criação de tabelas e agora começamos a colocar usuários dentro do nosso banco na primeira tabale uhuuuw

3 - Limpando Session
Agora chegou o momento de aprender a limpar os dados colocados no Session.
fazendo o comando session.rollback() Desfaz as alterações na sessão
e logo depois o session.commit() e assim ele vai excluir os dados

4 - Select
Agora como eu trago os dados que estão no banco de dados para a linguagem Python para fazer tratamentos
e fazer as verificações desses mesmo dados? Ou o que eu queira fazer atraves dessa linguagem Python?
Existem algumas formas e vamos aprender a primeira delas que é:
x1 = session.query(Pessoas) para trazer tudo de relacionado com os itens que tem dentro de Pessoas
resp: SELECT "Pessoa".id AS "Pessoa_id", "Pessoa".nome AS "Pessoa_nome", "Pessoa".usuario AS "Pessoa_usuario", 
"Pessoa".senha AS "Pessoa_senha" FROM "Pessoa"
já se utilizarmos com .all() podemos trazer o que tem no nosso banco de dados no caso all que é tudo.
x = x1.all() # Retorna todos os objetos da tabela Pessoas
resp: [<ORM.Pessoas object at 0x0000029F30805130>, <ORM.Pessoas object at 0x0000029F30805160>, <ORM.Pessoas object at 0x0000029F30805190>, 
<ORM.Pessoas object at 0x0000029F308051C0>, <ORM.Pessoas object at 0x0000029F308051F0>]
E vou aprender a trabalhar com esses tipos de dados.

O completo fica assim: x1 = session.query(Pessoas).all() # Retorna todos os objetos da tabela Pessoas
Gerando novamente o mesmo processo.

Gerando print(type(x[0])) vai nos mostra exatamente: <class 'ORM.Pessoas'> que é a própria class que é seu tipo
Agora usando print(x[0].id) com o ponto trazemos qualquer instancia dentro da nossa class no caso temos vários nesse Exemplo
no caso vamos usar o id vai trazer os id ou um apenas o primeiro. Caso alguma mudança traz outros tipos.
Outros tipos como id, nome, usuario, senha, etc. Se eu quero uma lista basta fazer um interavel do tipo
x = session.query(Pessoa).all()
for i in x:
    print(i.nome) ou outro que queira trazer

Posso ainda trazer apenas por filtro que eu desejo
x2 = session.query(Pessoas).filter(Pessoa.nome=='Lucas').first() # Retorna o primeiro objeto que atende ao filtro
nesse caso traz apenas o filtro para nome Lucas e sendo o primeiro que encontrar ou ainda Posso
x2 = session.query(Pessoas).filter(Pessoa.nome=='Lucas') para trazer todos com aquele nome, usando o interavel vai de id
vai vir apenas os id que tem aquele nome ou outras qualidades relacionadas aquele nome a esse filtro feito.
Uma forma analoga sem precisar trazer varios paramentros filter é só fazer: 
x3 = session.query(Pessoas).filter_by(nome='Lucas', usuario='Lukinha69') e colocar quantos paramentros quer trazer


5 - Or
Vamos fazer a importação do or que vai ser feito puxando essa biblioteca from sqlalchemy import or_
utilizando a mesma sintese que usavamos anteriormente, 
y = session.query(Pessoa).filter(or_(Pessoa.nome == 'Fabio', Pessoa.usuario == 'Cofabi123')).all()
o que traz tudo relacionado pelos interaveis quando colocamos o laço for para rodar vem todos que tem relação com
o que colocamos lá
for i in y:
    print(i.id) # Imprime o nome de cada objeto da lista y


6 - Update
Basicamente conseguimos mudar qualquer item de informação dentro do nosso banco de dados atraves do
Update, nesse caso vamos esta alterando apenas os nomes. Utilizando o mesmo recurso
y = session.query(Pessoas).filter(Pessoas.id == 5).all() com pequenas alterações.
Assim filtramos trazendo a informação relacionada ao id 5 e pela posição agora conseguimos mudar pelas
colunas, as posições agora colocam as colunas em cheque. y[0].nome = 'Jose' e adicionamos a baixo o nosso
session.commit() para que tudo seja adicionado corretamente, lembra um pouco o nesse aspecto na hora do commit
mudando assim o nome Fabio para Jose vamos tentar mudar agora nosso usuario também para jjfk02 da mesma linha
bastando fazer as alterações de y[0].nome = 'Jose' para y[0].usuario == 'jjfk02'
e assim para todos as alterações que eu desejo resolver.
y = session.query(Pessoas).filter(Pessoas.id == 5).all() # Retorna todos os objetos que atendem a pelo menos um dos filtros
y[0].nome = 'Joana' # Altera o nome do primeiro objeto da lista y
y[0].senha = 'novaSenha' # Altera a senha do primeiro objeto da lista y
y[0].usuario = 'jjkf02' # Altera o nome do primeiro objeto da lista y
session.commit() # Salva as alterações no banco de dados


7 - Delete
Agora é hora de aprender a deletar alguns valores da minha base de dados, antes aprendi a deletar a propria base de
dados agora aprender a deletar alguns valores dessa base de dados. É bem simples de se fazer:
z = session.query(Pessoas).filter(Pessoas.id == 3).delete() # Deleta o objeto que atende ao filtro
session.commit() # Salva as alterações no banco de dados
quando rodamos temos a exclusão da linha relacionada ao id == 3 e temos uma outra forma também recomendada que é:
# Deletando dados com filtro one()
z = session.query(Pessoas).filter(Pessoas.id == 3).one() # Deleta o objeto que atende ao filtro
session.delete(z) # Deleta o objeto da sessão
session.commit() # Salva as alterações no banco de dados


8, 9 e 10 - Criando Tabelas novas, Inserindo valores e ForingKey(Chave Estrangeira)
Criamos duas tabelas novas que são Categoria e Produto onde produtos faz referencia atraves da ForingKey 
a Categoria desse produto. Bastando incerir o id_categoria=numero da Categoria, um exemplo prático:
# Adicionando em produtos
x = Produto(produto='Cerveja', id_categoria=1)
y = Produto(produto='Banana', id_categoria=2)
z = Produto(produto='Maminha', id_categoria=3)
session.add_all([x, y, z]) # Adiciona todos os objetos à sessão
session.commit() # Salva as alterações no banco de dados

Mas claro que antes precisamos ter adicionado as categorias dessa forma
x = Categoria(nome='Frios', descricao='Tudo relacionado a frios')
y = Categoria(nome='Frutas', descricao='Frutas em geral')
z = Categoria(nome='Proteinas', descricao='Proteinas em geral')
session.add_all([x, y, z]) # Adiciona todos os objetos à sessão
session.commit() # Salva as alterações no banco de dados

Podemos assim ir inserindo mais produtos e mais categoria a eles. Logo faremos sistemas
onde será fácil de se fazer e implementar qualquer produto de forma bem completa no banco de dados


PROJETO PRÁTICO 2 - SISTEMA DE LOGIN 
Esse projeto vai esta no diretório de projeto e se chamará sistema de login
assim vamos dar continuidade a todo o projeto por lá e quaisquer informações
de aprendizado que eu considerar importantes e impertinentes estarei explicando aqui.

Esse projeto é constituido por 5 aulas do inicio ao fim.
Com cerca de 50 minutos com explicações 
Para a criptografia foi usado o char256 e por isso a String tem 100 caracteres

Inserimos ate o momento apenas uma tabelas com funcionalidade bem basica de cadastro e login, 
mas mais na frente seria interessante deixar mais robusto esse codigo, um pouco mais profissional
como tudo que eu faço.
O bom do estilo MVC é que podemos fazer a view da forma que quisermos mantendo tudo relacionado a o
controller e model ou outros.


| SOCKET | Criando aplicações através da rede
1, 2 - Enviando mensagem através da rede:
Antes de aprender desenvolvimento web, como criar paginas HTML e outros é necessário saber como funciona uma
rede e como enviar informação através da rede.
Como nosso professor não é um mestre em redes, vamos aprender a parte de programação relacionado a isso, 
e a parte de rede de forma mínima, mas nada impede de avançar mais depois sozinho.

Vamos montar um Client Service no diretório de projetos para rodar 100% do tempo onde vai receber uma imagem
e salvar em uma determinada pasta. O cliente vai ser um arquivo python que quando se roda esse arquivo ele enviar
uma imagem para um determinado servidor.
Criaremos um client.py e um server.py utilizando o import socket e rodando local


1 - Demonstração do projeto (CHAT) AULAS DE 1 A 4
Vamos montar uma especie de chat para ser utilizado por usuários diferentes usando os terminais.

O Chat é um pouco mais complexo mas ao mesmo tempo simples, site de grupos, vamos ter um dicionarios com cada um
desses dicionarios sendo uma sala, ou seja os elementos dentro será uma lista de pessoas que entraram
naquele chat específico. 
Ex: {sala_jogos: ['c1', 'c2', 'c3'],
    sala_games: ['..','..','..']} Mais ou menos isso para não dizer exatamente.

Com isso vamos ter um server que vai ficar responsavel de quando o c1 enviar mensagem o c2 e c3 receber essas
mensagem. E isso serve para todos os clientes dentro da sala e assim o servidor age com todos.

Vamos criar o servidor primeiro usando o socket para isso. 


DESENVOLVIMENTO WEB - API
1 - Teoria WEB:
Nesse modulo vamos começa a fazer nossos primeiros desenvolvimentos web, utilizando api.
Utilizando um framework python para construção de api's. E aprender como funciona esses sistemas web
aprendendo a usar uma ferramenta e o modo de aprender a aprender a usar qualquer ferramenta, com isso,
fica tudo mais fácil na hora de aprender qualquer outra ferramenta.
A ferramenta é o Fast API mas o principal é entender o raciocinio e a logica por de trás do que vamos 
construir, o que cada componente age dentro da rede.

Como funciona a rede?
Quando acessamos um site atraves de uma URL como exemplo o facebook estamos escrevendo um dominio que 
aponta para um determinado IP onde esse IP tem um determinado servidor que contém a dimensão de todas
aquelas informações que aquela rede proporciona. 
Quando enviamos uma informação para o IP estamos fazendo uma request também chamada requisição
e o site(IP - Servidor) nos manda de volta uma response que é uma resposta, formando assim um ciclo
de request -> response -> request -> response e assim sucessivamente enquanto esta utilizando o site/servidor.

Podemos criar uma unica api com várias funções que pode conectar com diferentes tipos de tecnologias para
diferentes dispositivos moveis ou 'fixos'.

2 - Primeira API
Colocando a mão na massa! Instalando pip install fastapi
vamos criar uma api de testes em testeAPI.py só para testes iniciais entendendo os mecanismos básicos
Para chamar uma determinada função da API deve ter um tipo de decorador que vai chamar a função da API
quando tal URL for acessada e cada uma vai ser acessada diferente dependendo do endpoint.

Como o Fast API usa o servidor do uvicorn vamos usa-lo para poder criar o servidor que vai fazer
aquelas trocas de infomações onde vou poder enviar e receber de acordo, para utilizar aqui na minha
maquina mesmo nesse primeiro momento. Chamando o uvicorn no terminal: uvicorn aplicação:instancia --reload 
uvicorn chama a parte de aplicação seria o .py que esta o app sem o .py e a instancia é chamar o decorador
já o --reload é para ficar rodando as alterações enquanto estou conectado, seria em tempo real.
E claro que para fazer tudo isso é necessário esta dentro da pasta que se encotra o .py que quer rodar.

Instalando o uvicorn:  pip install uvicorn

O decorador vai trazer para cada tipo de endpoint uma coisa difente para cada função ou funções que eu colocar
a baixo dele. No caso do nosso exemplo printou na tela uma mensagem com o decorador simples, vou deixar o codigo aqui:
from fastapi import FastAPI
app = FastAPI()

@app.get("/")
def root():
    return {"message": "Hello World"}

Printando na tela {"message": "Hello World"}

Com pequenas modificações ficamos assim para cada endpoint
@app.get("/home") # 127.0.0.1:8000/home
def root():
    return {"message": "Home"}
@app.get("/cadastro") # 127.0.0.1:8000/cadastro
def root():
    return {"message": "Cadastro"}
@app.get("/login") # 127.0.0.1:8000/login
def root():
    return {"message": "Login"}



3 - Consumindo API
Geralmente uma api não é feita para se ver atraves de um computador mas sim para fazer requisição
atraves da linguagem de programação executando alguma função sem precisar 'aparecer'.
Agora vamos montar outros exemplo e nesse caso vamos fazer um arquivo .py chamado req.py
para utilizar a biblioteca requests também que se instala assim: pip install requests.

Conseguimos saber os tipos de requisição que temos atraves do .json() e outros atraves da URL.

4 - Recebendo parâmetros
Vamos agora aprender a mandar paramentros e receber esses paramentros.
Começando a criar o nosso famoso def main para poder iniciar.
temos que 127.0.0.1:8000/docs já faz parte e serve para fazer alguns testes da biblioteca.
Fizemos algumas requisições do tipo get, agora vamos fazer algumas do tipo post.

usuario = [(1, "Lucas", "123456"), (2, "João", "123456"), (3, "Maria", "123456")]

#127.0.0.1:8000/docs
@app.get("/usuario/{id}")  # 
def main(id: int):
    for i in usuario:
        if i[0] == int(id):
            return {"id": i[0], "nome": i[1], "senha": i[2]}
    return {"message": "Usuario não encontrado"}


Agora vamos para o tipo post.
e com a alteração do req.py assim: 
# Post
retorno = requests.post('http://127.0.0.1:8000/usuario', params={'nome': 'Lucas'})
print(retorno.json())

# no testeAPI assim.
@app.post("/usuario")
def main(nome: str):
    for i in usuario:
        if i[1] == nome:
            return {"id": i[0], "nome": i[1], "senha": i[2]}
    return {"message": "Usuario não encontrado"}



5 - Pydantic 1
Ate agora esta sendo incrivel a questão das requisições vias post e get.
Pydantic é uma biblioteca Python utilizada para validação de dados, permitindo definir 
modelos com tipos e validações específicas. É usado para garantir que os dados recebidos em uma 
aplicação não tenham formato e tipo corretos, prevenindo erros e facilitando o tratamento de dados, 
especialmente em contextos como APIs e ETL. 

from pydantic import  # Para poder trazer e utilizar essa biblioteca

# Criamos um objeto usuário e assim já recebemos esse objeto como paramentro do nosso post
class Usuario(BaseModel):
    id: int
    nome: str
    senha: str

@app.post("/usuario")
def main(usuario: Usuario):
    return {"id": usuario.id, "nome": usuario.nome, "senha": usuario.senha} # Ou return usuario


6 - Pydantic 2
Vamos agora começar a simular como se fosse em um banco de dados, pois vamos trabalhar com Banco
de dados. Vamos continuar utilizando o codigo anterior mas nesse caso fazendo algumas adições.
Criamos assim uma lista utilizando o usuários. 
from pydantic import BaseModel

class Usuario(BaseModel):
    id: int
    nome: str
    senha: str

lista = [
    Usuario(id=1, nome="Lucas", senha="123456"),
    Usuario(id=2, nome="João", senha="123456"),
    Usuario(id=3, nome="Maria", senha="123456")
    ]

# Cadastra um novo usuario
@app.post("/usuario")
def main(usuario: Usuario):
    lista.append(usuario)
    return "usuario adicionado com sucesso"

# Mostra todos os usuarios cadastrados
@app.get("/usuarioListar")
def main():
    return lista



7 - PROJETO 1 - TODO (API)
Iniciaremos um novo projeto para revisar o que vimos ate agora sobre API.
Será uma TODO List o que traduzindo é uma lista de tarefas.
Primeiro vamos montar a estrutura que vai ser a nossa lista de tarefas
e criamos uma lista_tarefas = [] que vai ser preenchida consumindo uma instancia
da class Todo(BaseModel): isso para cada uma.
Nossa Todo:
# Criando a lista de tarefas
class Todo(BaseModel):
    tarefa: str
    objetivo: str
    realizada: bool
    prazo: Optional[datetime] = None

Agora vamos construir nosso primeiro endpoint será de inserir
chamamos a função para usar a Fast API como uvicorn todolist:app --reload
dentro da pasta que se encontra o .py que tem todas as instancias



8 - PROJETO 2 - AUTENTICAÇÃO + MYSQL criação da tabela (API)
Vai começar a ter uma cara mais profissional nossa api.
No caso utilizaremos o banco de dados do SQLite já que não conseguimos instalar
o Warmpserver por conta das autorizações de admin do sistema. Vamos lá!!
Fizemos as criações das nossas class/tabelas do banco de dados, os chamados e todas as
informações que precisamos para nossas tabelas.


9 - PROJETO 2 - AUTENTICAÇÃO + MYSQL CADASTRO
Agora é o momento de fazer a nossa API e desenvolve-la. 
Tivemos alguns testes que deram um pouco de problemas, pelo que aconteceu não estou conseguindo
achar o mesmo caminho do CONN em ambas aplicações.


10 - PROJETO 2 - AUTENTICAÇÃO + MYSQL LOGIN
Tivemos alguns problemas mas vamos consertar!!
Por alguma razão os parametros não estão reconhecendo a senha e a salvando!



FRONT-END|HTML
1 - O que vamos aprender?
Vamos aprender a criar paginas de internet com elementos HTML e todas essas formas. 
Nossa linguagem de backend é o python mas utilizaremos linguagens próprias como
HTML(linguagem de marcação), vamos utilizar o CSS para estilizar nossas pastas, 
javascript para algumas modalidades como movimentos e funcionalidades do nosso frontend.

O pyhton possui muitos frameworks para criação utilizando essas linguagens de estilos,
que se chama view. 

2 - Estrutura do arquivo
Para os aprendizados vamos criar uma pasta nova com o nome de frontend para aprender tudo
relacionado. Tudo que eu coloco abrindo chaves <tag> é chamado de tag e geralmente quando
abrimos nos fechamos também da mesma forma mas com uma barra </tag>
e tudo entre as tags esta dentro dos "poderes" da tag.

Abrimos a nossa primeira tag que é o <hmtl></html> onde se coloca todas as partes de dentro
da linguagem do html e dentro dividimos em duas partes que é o <head></head> que é o cabeçalho
e o <body></body> que representa o corpo do nosso documento html.
Dando um exemplo para se gravar bem o html seria o pai de head e body, o filho de head o title
e seria o neto de html e assim por diante de acordo com a arvore montada da estrutura.
E cada tipo de tag vai ter uma função diferente e cada uma pode conter determinadas individualidades
que será chamada de 


3 - Títulos
Vamos focar em cada parte mas vou colocando tudo que eu for aprendendo aqui no nosso corpo tudo bem?
E quem sabe ate no corpo do nosso site, ate porque estamos aqui aprendendo e deve ser bem aprendido.
Tudo dentro do HTML serve apenas para indicar cada parte e cada lugar, para criar os estilos vamos ter
que utilizar o nosso CSS e que vamos aprender no decorrer dos nossos passos aqui dentro.
Olá tudo bem? Como vai você?
        <h1>Meu Titulo</h1>
        <h2>Meu Titulo</h2>
        <h3>Meu Titulo</h3>
        <h4>Meu Titulo</h4>
        <h5>Meu Titulo</h5>
        <h6 style="font-size: 50;">Meu Titulo</h6> Fica como maior mas não o mais importante na pagina


4 - Parágrafos e divisões
DEntro do nosso body
<h1>O que é Java</h1>
<h3>Java é pior linguagem de programação.</h3>
<p>Java é chato para um caralho.</p>
Mas podemos ainda dividir os dois blocos de paragrafos usando a nossa div.
Basicamente simples, usamos também o section e estilizamos só para dar uma destacada.


5, 6 - Links
Dentro de uma pagina existem diversos links que vão nos mandar para outras partes do site ou
ate para outros sites inclusive. Como podemos fazer para criar esses links dentro do nosso projeto?
E qual a maneira correta de se fazer isso? A maneira correta seria: 
<a href="https://www.python.org/">Mais informações sobre Python</a> Assim temos um exemplo prático.
pademos também redirecionar para outra pagina que estamos desenvolvendo também de forma
separada. 


7 - Imagens
Aprendendo a colocar imagens no nosso HTML que pode ser de duas formas, a primeira é colocar
um link ate uma imagem e a outra é colocar uma imagem que esta no computador ou servidor que leva
ate aquela imagem. E podemos colocar atraves da tag <img> exemplo:
<img src="https://files.tecnoblog.net/wp-content/uploads/2022/12/python-ari-joury-3.jpg" alt="Python" width="300" height="200">
 

8 - Tabelas
Criando tabelas de maneira fácil de direta usando.
<hr> Uma linha horizontal passando.
<table></table> Cria um tipo de tabela. E na tabela precisa definir quantas linhas vão ter
atraves do <td> e temos também o <th>
Diferença entre th e td é que th é como se fosse em negrito e td não.
Podemos também usar como parametro o colspan="quantos ocupa" para determinar quantos ocupam,
dentro de uma determinada tabela. 


9 - Listas
Criação de listas. Para a criação de listas usamos o <ul></ul>
<ul></ul> Para criação de listas não ordenada
<ol></ol> Para criação de uma lista ordenada
<li></li> Cria as linhas das listas com pontos no lado esquerdo.
E dentro ainda das linhas <li></li> podemos colocar outras tags para outras finalidades.


10 - Barra de navegação
Vamos fazer um cabeçalho bem feito e para isso vamos abrir um outro arquivo que vamos chamar
de page_exemple1.html
Quem esta atualizado no html moderno é bom esta sempre atualizado com o header, section e footer.
cabeçalho, sessão e rodapé.  


11 - Formulários
Agora vamos criar um formulário onde vamos colocar o registro e vai ter um login
que vai conversar com algum servidor que vamos fazer também e e que vai se refletir
dentro da nossa aplicação do frontend conectando com o backend.

E dentro do nosso backend podemos fazer qualquer tipo de linguagem de programação
no caso vamos usar o python.
Colocamos a tag <form> para criar nosso formulário. Adicionamos um método
dentro do form. method="POST" não é enviado atraves de uma URL e o GET é  enviado
através de uma URL. E action="local onde enviamos a informação".
Estamos vendo várias variações dentro do types dentro do input, de várias formas
diferentes. Testamos os seguintes ate agora e vamos aprender mais. 
    <div>
        <form>
           <input type="text" name="Nome" placeholder="Nome">
           <input type="password" name="Senha" placeholder="Senha">
           <input type="number" name="Idade" placeholder="Idade">
           <input type="color" name="Cor" placeholder="Cor">
           <input type="email" name="Email" placeholder="Email">
           <input type="date" name="Data" placeholder="Data">
           <input type="submit" value="Enviar Clica aqui!">
           <input type="file" name="Arquivo" placeholder="Arquivo">
           <input type="checkbox" name="Aceito" value="Aceito"> Aceito os termos
           <input type="radio" name="Sexo" value="Masculino"> Masculino
           <input type="radio" name="Sexo" value="Feminino"> Feminino
        </form>
    </div>


12 - Formulários continuação...
Temos aprensentado agora o campo <textarea> Para criar uma area para digitar qualquer coisa.
<div aling="center">
        <form aling="center" method="post">
           Nome:
              <br>
              <input type="text" name="nome" placeholder="Digite seu nome" required>
              <br>
              <br>
           Senha:
            <br>
                  <input type="password" name="senha" placeholder="Digite sua senha" required>
            <br>
            <br>
                  <input type="submit" value="Enviar"> 
            <br>
            <br>
            Cidade:
            <br>
            <select name="cidade">
                <option value="F">Franca</option>
                <option value="SP">São Paulo</option>
                <option value="RP">Ribeirão Preto</option>
            </select>
            <br>
            <br>
            Sexo:
            <input type="radio" name="sexo" value="M">Masculino
            <input type="radio" name="sexo" value="F">Feminino
            <br>

        </form>
    </div>



FRONTEND | CSS
1 - CSS inline




O que faz cada tag
<title></title> Define o titulo que vai esta lá na parte da pagina.
<h1></h1> Define um titulo onde eu coloca-lo que tem um tamanho h1 > h2 > h3 > hn 
Mostrando o nivel de importancia não necessariamente de tamanho
<div></div> Indica uma divisão entre partes do body que podem ser e ter regras e estilos
diferentes assim como formatos sem uma atrapalhar a outra.
<section></section> Também divide mas é um pouco mais moderninho a sua forma de estilizar.
<a href="site.com.br"></a> Serve para referenciar um link dentro de um texto.
<img> tag para colocar ou apontar uma determinada imagem.
<hr> Uma linha horizontal passando.
<table></table> Cria um tipo de tabela. E na tabela precisa definir quantas linhas vão ter
atraves do <td> e temos também o <th>
Diferença entre th e td é que th é como se fosse em negrito e td não.
<ul></ul> Para criação de listas não ordenada
<ol></ol> Para criação de uma lista ordenada
<li></li> Cria as linhas das listas com pontos no lado esquerdo.
<tr></tr> É uma linha quando criamos uma linha.
<td></td> É quando criamos uma coluna Principalmente quando é dentro da linha
<form> Tudo que esta dentro dessa tag esta dentro de um formulário.

